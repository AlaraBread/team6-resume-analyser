{
	"name": "resume_analyzer",
	"version": "1.0.0",
	"description": "",
	"main": "",
	"scripts": {
		"dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\"",
		"dev:frontend": "cd frontend && npm run dev",
		"dev:backend": "cd backend && deno run --allow-net --allow-env --watch main.ts",
		"format": "prettier -uw \"**/*\""
	},
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"concurrently": "^9.1.0",
		"prettier": "^3.3.3"
	}
}
